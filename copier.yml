project_name:
    type: str
    help: What is the package name?

has_CLI:
    type: bool
    help: Does the package have CLI commands?
    default: true

data_dir:
    type: bool
    help: Should a data directory be created?
    default: false

ci_workflows:
    type: bool
    help: Should GitHub Actions CI workflows be created?
    default: true